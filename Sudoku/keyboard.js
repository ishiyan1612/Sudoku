function handleKeydown(e) {

    var key = e.key;
    //var result = false;
    //alert(key);               //e.key確認用

    //mode = 1;  
    /*  
    if (mode == 1) {
        if (key === 'ArrowRight') { // 右
            result = Shift(0, 1);
        } else if (key === 'ArrowLeft') { // 左
            result = Shift(0, -1);
        } else if (key === 'ArrowDown') { //下
            result = Shift(1, 0);
        } else if (key === ' ') { //スペース（回転）
            result = rotationBlock();
        }
    }

    if (result) draw();
    */
}